ci: scope markdownlint to main README only

Legacy course documentation (CHANGELOG, CONTRIBUTING, EXAMPLES,
QUICKSTART, N8N_INTEGRATION, INFO/SETUP/PROFILE helpers, etc.)
predates the linting setup and carries many inherited formatting
issues that are not worth rewriting for every document.

Scope the CI markdownlint job to README.md, which is the canonical
public entry point we actively maintain. Also drop the unused
.markdownlintignore file (the negative-globs approach is now
inline in the workflow and only one file is linted).
